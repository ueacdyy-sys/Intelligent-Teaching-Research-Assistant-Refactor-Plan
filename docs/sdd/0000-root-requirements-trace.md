# SDD 0000: Root Requirements Trace

## Authoritative Source

The authoritative product source is:

`C:\Users\Administrator\Desktop\智能教研助手\项目根本需求（禁止改动）`

The HTML architecture board is a derived planning artifact, not the source of truth.

## Product Capabilities

| Capability | Requirement Summary | Refactor Boundary |
| --- | --- | --- |
| Teaching mode | Quiz, AI grading, attendance, resources, tutoring, archive materials, student profiles | Teaching subsystem |
| Research mode | Multi-model conversation, node management, bookmarks, knowledge base, training, device collaboration | Research subsystem |
| Student app | Login, AI tutor, student archive, teaching materials, personalized question bank, scan-to-answer | Student subsystem |
| Agent mode | Orchestrating agent, teaching/research sub-agents, external app control, workflow/plugin generation | Agent subsystem + Harness |
| Knowledge isolation | Public/private knowledge bases, different access rights for cloud/local/remote nodes | Data platform + policy |
| Workflow and plugins | AI generates workflow/plugin code, tests it, reports failure, waits for human approval | Workflow engine + Harness |
| Packaging and runtime | Small package, stable desktop operation, efficient concurrency | Rust local runtime + Go hot services |

## Non-Negotiable Invariants

- Private student and private knowledge data must not be sent to a cloud node without policy approval.
- External application control must pass through Agent Harness.
- AI-generated workflows/plugins are draft-only until tests and human approval pass.
- Training/model dependencies are optional worker dependencies, not baseline runtime dependencies.
- New language modules require contracts, tests, performance evidence, and rollback.

## Current Trace Slices

| Slice | Root Capability | Evidence Boundary |
| --- | --- | --- |
| SDD 0315 published search foundation | Student app, teaching materials, student archive | `GET /v1/student-app/archive-items?query=` searches title/tags only inside the Student App visible publication projection. |
| SDD 0316 published detail metadata read | Student app, teaching materials, student archive | `GET /v1/student-app/archive-items/{archiveItemId}` returns only safe metadata for the authenticated student's own published archive item and excludes `contentRef`, publication internals, answers, model output, and worker state. |
| SDD 0317 published content preview precheck | Student app, teaching materials, student archive | Blocks Student App published-material content preview until a safe preview store and renderer exist; consumes 0316 metadata evidence and does not read raw content or expose `contentRef`. |
| SDD 0318 published content preview read foundation | Student app, teaching materials, student archive | Adds `GET /v1/student-app/archive-items/{archiveItemId}/content-preview` over a dedicated safe preview table and Student App publication projection filter; does not expose `contentRef`, raw content, OCR/RAG chunks, answers, model output, or worker/publication internals. |
| SDD 0319 published content preview render envelope | Student app, teaching materials, student archive | Adds `GET /v1/student-app/archive-items/{archiveItemId}/content-preview/rendered` over the 0318 safe preview read foundation; emits `SAFE_TEXT_BLOCKS` only and still excludes raw content, `contentRef`, HTML/Markdown, OCR/RAG chunks, answers, model output, and worker/publication internals. |
| SDD 0320 student app archive item study packet | Student app, teaching materials, student archive | Adds `GET /v1/student-app/archive-items/{archiveItemId}/study-packet` to combine safe published metadata with the 0319 `SAFE_TEXT_BLOCKS` preview; excludes `studentId`, `contentRef`, raw/full content, HTML/Markdown, OCR/RAG chunks, answers, model output, and worker/publication internals. |
| SDD 0321 student app archive item learning actions | Student app, AI tutor, personalized question bank, student archive | Adds `GET /v1/student-app/archive-items/{archiveItemId}/learning-actions` to expose safe action affordances for a READY study packet; actions point only to the existing AI tutor request queue and do not expose prompts, answers, `contentRef`, preview content, OCR/RAG chunks, model output, or worker/publication internals. |
| SDD 0322 student app ai tutor published learning action source | Student app, AI tutor, personalized question bank, teaching materials | Extends `POST /v1/student-app/ai-tutor-requests` with optional `learningActionSource` so material-detail actions must prove the 0321 READY learning-action boundary before queue admission; it still creates only the existing tutor request queue item and does not expose preview content, prompts, OCR/RAG chunks, answers, model output, or worker/publication internals. |
| SDD 0323 student app ai tutor worker study packet input | Student app, AI tutor, personalized question bank, teaching materials | Adds worker-only `POST /v1/teaching/tutoring-analysis-requests/{requestId}/ai-tutor-study-packet-input` so a claimed StudentTutorAgent worker must prove the request lease and rebuild the READY SAFE_TEXT_BLOCKS study packet before future model input construction; it does not run models, generate drafts, expose raw content, `contentRef`, prompts, OCR/RAG chunks, answers, or model output. |
| SDD 0324 student app ai tutor model execution precheck | Student app, AI tutor, personalized question bank, teaching materials | Adds an idempotent `StudentAppAITutorModelExecutionPrecheckPort.recordModelExecutionPrecheck` runtime that consumes the 0323 worker-safe study packet input evidence, requires reviewed approval and queue-only policy, hashes safe blocks, and records future model execution admission without starting model inference, constructing prompts, writing tutor results, exposing raw content, `contentRef`, OCR/RAG chunks, answers, or model output. |
| SDD 0325 student app ai tutor controlled answer artifact | Student app, AI tutor, personalized question bank, teaching materials | Adds an idempotent `StudentAppAITutorControlledAnswerArtifactPort.recordControlledAnswerArtifact` runtime that consumes the 0324 model execution precheck evidence and records a review-only sanitized answer artifact without persisting tutoring results, publishing student-visible content, exposing raw model output, prompts, answer keys, `contentRef`, OCR/RAG chunks, direct DB fields, or internal errors. |
| SDD 0326 student app ai tutor answer review gate | Student app, AI tutor, personalized question bank, teaching materials | Adds an idempotent `StudentAppAITutorAnswerReviewGatePort.recordAnswerReviewGate` runtime that consumes the 0325 controlled answer artifact evidence and records a teacher/admin human-review gate using section hashes and metadata only, without sending guidance text to the port, persisting tutoring results, publishing student-visible content, or exposing raw model output, prompts, answer keys, `contentRef`, OCR/RAG chunks, direct DB fields, or internal errors. |
| SDD 0327 student app ai tutor reviewed result persistence bridge | Student app, AI tutor, personalized question bank, teaching materials | Adds an idempotent reviewed-result persistence bridge that consumes the 0326 answer review gate and calls the existing `StudentAppAITutorResultPort.recordTutoringAnalysisResult` / `RecordTutoringAnalysisResult.Execute` boundary with metadata and hashes only, without direct DB access, HTTP, tools, Swarm, student-visible publication, guidance text, raw model output, prompts, answer keys, `contentRef`, OCR/RAG chunks, raw result refs, or internal errors. |
| SDD 0328 student app ai tutor result student visibility review | Student app, AI tutor, student archive safety, teaching materials | Adds an idempotent human student-visibility review that consumes the 0327 reviewed-result persistence bridge and records `AI_TUTOR_RESULT_STUDENT_VISIBILITY_APPROVED_NOT_DELIVERED` through an injected port only, without publishing to students, creating delivery envelopes, durable archive persistence, direct DB access, HTTP, tools, Swarm, guidance text, raw model output, prompts, answer keys, `contentRef`, OCR/RAG chunks, raw result refs, or internal errors. |
| SDD 0329 student app ai tutor result student delivery envelope | Student app, AI tutor, student archive safety, teaching materials | Adds an idempotent Student App renderable AI Tutor result delivery envelope that consumes the 0328 visibility review and 0325 controlled answer artifact, recomputes the guidance sections hash, and records `STUDENT_APP_AI_TUTOR_RESULT_STUDENT_DELIVERY_ENVELOPE_READY_NOT_PERSISTED` through an injected port only, without durable archive persistence, direct DB access, HTTP, model inference, OCR/RAG, tools, Swarm, raw model output, prompts, answer keys, `contentRef`, raw result refs, internal errors, or archive-row writes. |
| SDD 0330 student app ai tutor result student archive persistence command | Student app, AI tutor, student archive safety, teaching materials | Adds an idempotent append-only student archive persistence command that consumes the 0329 delivery envelope and 0325 controlled answer artifact, recomputes the guidance sections hash, and records `STUDENT_APP_AI_TUTOR_RESULT_STUDENT_ARCHIVE_PERSISTENCE_COMMAND_RECORDED_NOT_COMMITTED` without durable archive commit, direct DB access, HTTP, model inference, OCR/RAG, tools, Swarm, raw model output, prompts, answer keys, `contentRef`, raw result refs, internal errors, or archive-row writes. |
| SDD 0331 student app ai tutor result student archive storage commit | Student app, AI tutor, student archive safety, teaching materials | Adds an idempotent storage commit runtime that consumes the 0330 persistence command and commits safe guidance to Teaching Archive through the injected `TeachingArchiveCreateItemPort.createArchiveItem` / `CreateArchiveItem.ExecuteWithPersistence` boundary, recording `STUDENT_APP_AI_TUTOR_RESULT_STUDENT_ARCHIVE_STORAGE_COMMITTED` without JS direct DB access, HTTP, model inference, OCR/RAG, tools, Swarm, raw model output, prompts, answer keys, `contentRef`, raw result refs, internal errors, or row verification claims. |
| SDD 0332 student app ai tutor result student archive row verification | Student app, AI tutor, student archive safety, teaching materials | Adds an idempotent row verification runtime that consumes the 0331 storage commit and verifies the committed safe-guidance archive item through injected `TeachingArchiveRowReadPort.getArchiveItemById` / `ArchiveRepository.GetByID`, recording `STUDENT_APP_AI_TUTOR_RESULT_STUDENT_ARCHIVE_PHYSICAL_ROW_VERIFIED` without JS direct DB/SQL access, HTTP, model inference, OCR/RAG, tools, Swarm, raw model output, prompts, answer keys, `contentRef`, raw result refs, internal errors, or new writes. |
| SDD 0333 student app ai tutor result student archive read | Student app, AI tutor, student archive safety, teaching materials | Adds a Student App safe result-card read boundary that consumes the 0332 row verification and exposes `GET /v1/student-app/archive-items/{archiveItemId}/ai-tutor-result` through `ReadStudentAppAITutorResultArchive.Execute` / `ArchiveRepository.GetStudentAppAITutorResultArchiveSnapshot`, recording `STUDENT_APP_AI_TUTOR_RESULT_STUDENT_ARCHIVE_READ_VERIFIED` without exposing `contentRef`, raw result refs, raw model output, prompts, answer keys, worker internals, direct JS DB/SQL access, HTTP, model inference, OCR/RAG, tools, Swarm, or unsafe rendering. |
| SDD 0334 student app ai tutor result student archive render | Student app, AI tutor, student archive safety, teaching materials | Adds a Student App safe render-envelope boundary that consumes the 0333 result-card read and exposes `GET /v1/student-app/archive-items/{archiveItemId}/ai-tutor-result/rendered` through `RenderStudentAppAITutorResultArchive.Execute`, recording `STUDENT_APP_AI_TUTOR_RESULT_STUDENT_ARCHIVE_RENDER_VERIFIED` as `SAFE_TEXT_BLOCKS` with `SUMMARY` and `GUIDANCE_SECTION` blocks while excluding `contentRef`, raw result refs, raw model output, prompts, answer keys, worker internals, rendered HTML, rendered Markdown, direct JS DB/SQL access, HTTP, model inference, OCR/RAG, tools, or Swarm. |
| SDD 0335 student app ai tutor result student archive learning actions | Student app, AI tutor, personalized question bank, student archive safety | Adds a Student App result-archive learning-actions boundary that consumes the 0334 safe render envelope and exposes `GET /v1/student-app/archive-items/{archiveItemId}/ai-tutor-result/learning-actions` through `ReadStudentAppAITutorResultArchiveLearningActions.Execute`, recording `STUDENT_APP_AI_TUTOR_RESULT_STUDENT_ARCHIVE_LEARNING_ACTIONS_VERIFIED` with `AI_TUTOR_RESULT_ARCHIVE` learning-action sources that target only `POST /v1/student-app/ai-tutor-requests`; queue admission rebuilds the safe archive card, render envelope, and actions before creating a request while excluding raw render blocks, `contentRef`, raw result refs, raw model output, prompts, answer keys, worker internals, direct JS DB/SQL access, HTTP, model inference, OCR/RAG, tools, or Swarm. |
| SDD 0336 student app ai tutor worker result archive input | Student app, AI tutor, personalized question bank, student archive safety | Extends the worker-only `POST /v1/teaching/tutoring-analysis-requests/{requestId}/ai-tutor-study-packet-input` boundary so persisted `AI_TUTOR_RESULT_ARCHIVE` queue sources rebuild the safe result archive card, render envelope, and learning actions before returning worker-safe `SAFE_TEXT_BLOCKS`; persists `source_type` on tutoring requests while keeping student-facing request responses free of source internals and excluding published-preview reads, raw refs, prompts, model output, OCR/RAG, answer keys, tools, HTTP, or Swarm. |
| SDD 0337 student app ai tutor result archive model execution precheck | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive-sourced queue-only model execution precheck that consumes the 0336 worker result-archive input and records `STUDENT_APP_AI_TUTOR_RESULT_ARCHIVE_MODEL_EXECUTION_PRECHECKED` through the shared `student_app_ai_tutor_model_execution_precheck_runtime`; it hashes safe result archive blocks and source refs into the input hash while sending only source, render format, block count, block digests, approval, and evidence metadata to the injected port, without sending text/source refs to the port, constructing prompts, running models, recording tutoring results, publishing student-visible content, direct DB/HTTP access, OCR/RAG, tools, or Swarm. |
| SDD 0338 student app ai tutor result archive controlled answer artifact | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive-sourced controlled answer artifact audit that consumes READY 0337 evidence and records `STUDENT_APP_AI_TUTOR_RESULT_ARCHIVE_CONTROLLED_ANSWER_ARTIFACT_RECORDED` through the shared `student_app_ai_tutor_controlled_answer_artifact_runtime`; output keeps `learningActionSource=AI_TUTOR_RESULT_ARCHIVE`, `resultArchiveStatus=READY_FOR_STUDENT_APP_READ`, and `PENDING_HUMAN_REVIEW` while blocking tutoring result persistence, student-visible publication, raw model output, prompts, answer keys, direct DB/HTTP access, OCR/RAG, tools, or Swarm. |
| SDD 0339 student app ai tutor result archive answer review gate | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive-sourced answer review gate audit that consumes READY 0338 evidence and records `STUDENT_APP_AI_TUTOR_RESULT_ARCHIVE_ANSWER_REVIEW_GATE_RECORDED` through the shared `student_app_ai_tutor_answer_review_gate_runtime`; output keeps `learningActionSource=AI_TUTOR_RESULT_ARCHIVE` and `resultArchiveStatus=READY_FOR_STUDENT_APP_READ` while sending only review metadata and hashes to the injected port, blocking guidance text leakage, tutoring result persistence, student-visible publication, raw model output, prompts, answer keys, direct DB/HTTP access, OCR/RAG, tools, or Swarm. |
| SDD 0340 student app ai tutor result archive reviewed result persistence bridge | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive-sourced reviewed result persistence bridge audit that consumes READY 0339 evidence and records `STUDENT_APP_AI_TUTOR_RESULT_ARCHIVE_REVIEWED_RESULT_PERSISTED` through the shared `student_app_ai_tutor_reviewed_result_persistence_bridge_runtime` and existing `RecordTutoringAnalysisResult.Execute` use case; output preserves `learningActionSource=AI_TUTOR_RESULT_ARCHIVE` and `resultArchiveStatus=READY_FOR_STUDENT_APP_READ` while blocking guidance text leakage, student-visible publication, raw model output, prompts, answer keys, raw refs, direct DB/HTTP access, OCR/RAG, tools, or Swarm. |
| SDD 0341 student app ai tutor result archive student visibility review | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive-sourced student visibility review audit that consumes READY 0340 evidence and records `STUDENT_APP_AI_TUTOR_RESULT_ARCHIVE_STUDENT_VISIBILITY_REVIEW_RECORDED` through the shared `student_app_ai_tutor_result_student_visibility_review_runtime`; output preserves `learningActionSource=AI_TUTOR_RESULT_ARCHIVE` and `resultArchiveStatus=READY_FOR_STUDENT_APP_READ` while approving only a future delivery runtime and blocking guidance text leakage, delivery envelope creation, student-visible publication, raw model output, prompts, answer keys, raw refs, direct DB/HTTP access, OCR/RAG, tools, or Swarm. |
| SDD 0342 student app ai tutor result archive student delivery envelope | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive-sourced student delivery envelope audit that consumes READY 0341 and READY 0338 evidence and records `STUDENT_APP_AI_TUTOR_RESULT_ARCHIVE_STUDENT_DELIVERY_ENVELOPE_READY_NOT_PERSISTED` through the shared `student_app_ai_tutor_result_student_delivery_envelope_runtime`; output preserves `learningActionSource=AI_TUTOR_RESULT_ARCHIVE` and `resultArchiveStatus=READY_FOR_STUDENT_APP_READ`, creates only a safe Student App render envelope, and blocks durable archive persistence, main DB writes, raw refs, raw model output, prompts, answer keys, direct DB/HTTP access, OCR/RAG, tools, or Swarm. |
| SDD 0343 student app ai tutor result archive student archive persistence command | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive-sourced student archive persistence command audit that consumes READY 0342 and READY 0338 evidence and records `STUDENT_APP_AI_TUTOR_RESULT_ARCHIVE_STUDENT_ARCHIVE_PERSISTENCE_COMMAND_RECORDED_NOT_COMMITTED` through the shared `student_app_ai_tutor_result_student_archive_persistence_command_runtime`; output preserves `learningActionSource=AI_TUTOR_RESULT_ARCHIVE` and `resultArchiveStatus=READY_FOR_STUDENT_APP_READ`, records only an append-only command, and blocks durable storage commit, main DB writes, raw refs, raw model output, prompts, answer keys, direct DB/HTTP access, OCR/RAG, tools, or Swarm. |
| SDD 0344 student app ai tutor result archive student archive storage commit | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive-sourced student archive storage commit audit that consumes READY 0343 evidence and commits safe guidance through the shared `student_app_ai_tutor_result_student_archive_storage_commit_runtime` via injected `TeachingArchiveCreateItemPort.createArchiveItem`; output records `STUDENT_APP_AI_TUTOR_RESULT_ARCHIVE_STUDENT_ARCHIVE_STORAGE_COMMITTED`, preserves `learningActionSource=AI_TUTOR_RESULT_ARCHIVE` and `resultArchiveStatus=READY_FOR_STUDENT_APP_READ`, requires persisted Teaching Archive outcome, blocks direct DB/HTTP/model/RAG/tool/Swarm access, and leaves physical row verification to the next reviewed slice. |
| SDD 0345 student app ai tutor result archive student archive row verification | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive-sourced student archive physical row verification audit that consumes READY 0344 evidence and verifies `tarch_student_ai_tutor_result_001` through the shared `student_app_ai_tutor_result_student_archive_row_verification_runtime` via injected `TeachingArchiveRowReadPort.getArchiveItemById`; output records `STUDENT_APP_AI_TUTOR_RESULT_ARCHIVE_STUDENT_ARCHIVE_PHYSICAL_ROW_VERIFIED`, preserves `learningActionSource=AI_TUTOR_RESULT_ARCHIVE` and `resultArchiveStatus=READY_FOR_STUDENT_APP_READ`, requires `ArchiveRepository.GetByID` row-shape evidence, blocks direct DB/HTTP/model/RAG/tool/Swarm access, and leaves safe Student App read to the next reviewed slice. |
| SDD 0346 student app ai tutor result archive student archive read | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive-sourced Student App safe read audit that consumes READY 0345 evidence and reads the archived result through the shared `student_app_ai_tutor_result_student_archive_read_runtime` via injected `StudentAppAITutorResultArchiveReadPort.readStudentVisibleArchivedResult`; output records `STUDENT_APP_AI_TUTOR_RESULT_ARCHIVE_STUDENT_ARCHIVE_READ_VERIFIED`, preserves `learningActionSource=AI_TUTOR_RESULT_ARCHIVE` and `resultArchiveStatus=READY_FOR_STUDENT_APP_READ`, reuses `ReadStudentAppAITutorResultArchive.Execute` and the existing `GET /v1/student-app/archive-items/{archiveItemId}/ai-tutor-result` contract, blocks `contentRef`, raw refs, raw model output, prompts, answer keys, direct DB/HTTP/model/RAG/tool/Swarm access, and leaves safe render to the next reviewed slice. |
| SDD 0347 student app ai tutor result archive student archive render | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive-sourced Student App safe render audit that consumes READY 0346 evidence and renders the archived result through the shared `student_app_ai_tutor_result_student_archive_render_runtime` via injected `StudentAppAITutorResultArchiveRenderPort.renderStudentVisibleArchivedResult`; output records `STUDENT_APP_AI_TUTOR_RESULT_ARCHIVE_STUDENT_ARCHIVE_RENDER_VERIFIED`, preserves `learningActionSource=AI_TUTOR_RESULT_ARCHIVE` and `resultArchiveStatus=READY_FOR_STUDENT_APP_READ`, reuses `RenderStudentAppAITutorResultArchive.Execute` and the existing `GET /v1/student-app/archive-items/{archiveItemId}/ai-tutor-result/rendered` contract, emits only `SAFE_TEXT_BLOCKS`, blocks `contentRef`, raw refs, raw model output, prompts, answer keys, rendered HTML, rendered Markdown, direct DB/HTTP/model/RAG/tool/Swarm access, and leaves result-archive learning actions to the next reviewed slice. |
| SDD 0348 student app ai tutor result archive student archive learning actions | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive-sourced Student App learning-actions audit that consumes READY 0347 render evidence, reuses the shared `student_app_ai_tutor_result_student_archive_learning_actions_runtime` and injected `StudentAppAITutorResultStudentArchiveLearningActionsPort.readStudentVisibleArchivedResultLearningActions`, records `STUDENT_APP_AI_TUTOR_RESULT_ARCHIVE_STUDENT_ARCHIVE_LEARNING_ACTIONS_VERIFIED`, preserves `learningActionSource=AI_TUTOR_RESULT_ARCHIVE`, `resultArchiveStatus=READY_FOR_STUDENT_APP_READ`, and `SAFE_TEXT_BLOCKS`, returns only action affordances targeting `POST /v1/student-app/ai-tutor-requests`, blocks raw render blocks, `contentRef`, raw refs, raw model output, prompts, answer keys, rendered HTML/Markdown, direct DB/HTTP/model/RAG/tool/Swarm access, and keeps follow-up model execution to later reviewed slices. |
| SDD 0349 student app ai tutor result archive follow-up queue admission | Student app, AI tutor, personalized question bank, student archive safety | Adds a result-archive follow-up queue-admission audit that consumes READY 0348 learning-actions evidence and verifies the existing `POST /v1/student-app/ai-tutor-requests` path; `CreateStudentAppAITutorRequest.Execute` must rebuild the archive item, safe result archive card, `SAFE_TEXT_BLOCKS` render envelope, and learning actions before creating the queued tutoring request, preserving `AI_TUTOR_RESULT_ARCHIVE` provenance while blocking new endpoints, raw render blocks, `contentRef`, raw refs, raw model output, prompts, answer keys, rendered HTML/Markdown, direct JS DB/HTTP/model/RAG/tool/Swarm access, or student-response leakage of source internals. |
| SDD 0350 student app ai tutor result archive follow-up worker continuity | Student app, AI tutor, personalized question bank, student archive safety | Adds a follow-up worker-continuity audit that consumes READY 0349 queue-admission evidence and proves the queued `AI_TUTOR_RESULT_ARCHIVE` source can continue through the existing leased worker claim, worker-only study-packet-input endpoint, and result-archive model precheck path; Go usecase regression now covers a distinct follow-up archive item id through `GetByID` and `GetStudentAppAITutorResultArchiveSnapshot`, blocking published-preview fallback, new endpoints, raw refs, prompts, model output, answers, direct JS DB/HTTP/model/RAG/tool/Swarm access, or student-facing source leakage. |
| SDD 0351 student app ai tutor result archive follow-up depth budget guard | Student app, AI tutor, personalized question bank, student archive safety | Adds a bounded follow-up depth audit that consumes READY 0350 worker-continuity evidence and caps `AI_TUTOR_RESULT_ARCHIVE` follow-ups at depth 2; snapshot/read/render/actions expose server-computed `followUpDepth`, max-depth archives return zero follow-up actions, queue admission rejects tampered or max-depth sources, tutoring requests persist `source_follow_up_depth`, worker input revalidates the persisted depth against regenerated actions, and OpenAPI requires result-archive depth while forbidding it for published study-packet sources. |
| SDD 0352 student app ai tutor result archive follow-up queue idempotency guard | Student app, AI tutor, personalized question bank, student archive safety, write-path performance | Adds a pending follow-up idempotency audit that consumes READY 0351 depth-budget evidence and coalesces duplicate `AI_TUTOR_RESULT_ARCHIVE` follow-up submits at the same archive item, requesting principal, question-bank intent, follow-up depth, and student scope; queued/in-progress requests return the existing request, terminal requests can create a new one, and PostgreSQL adds a partial unique index for pending follow-up rows to reduce retry/write amplification under high concurrency. |
| SDD 0353 student app ai tutor result archive follow-up lineage guard | Student app, AI tutor, personalized question bank, student archive safety, evidence chain | Adds a follow-up lineage audit that consumes READY 0352 idempotency evidence and requires safe AI Tutor result archive snapshots to preserve `source_archive_item_id` and internal `source_tutoring_analysis_request_id`; read/render/actions expose only safe `sourceArchiveItemId`, worker-only input receives `resultArchiveSourceItemId`, self/missing lineage is rejected, and the slice keeps raw result refs, internal request ids, model output, OCR/RAG, Swarm, tools, direct JS database access, and student-facing internal metadata out of the public path. |
| SDD 0354 student app ai tutor request progress timeline | Student app, AI tutor, personalized question bank, student archive safety, mobile progress UI | Adds a Student App safe request-progress audit that consumes READY 0353 lineage evidence and changes `GET /v1/student-app/ai-tutor-requests` from the generic internal tutoring-request list to `StudentAppAITutorRequestProgressListResponse`; domain maps student-owned requests into progress stage, next action, safe status message, and a fixed four-step timeline while blocking `resultRef`, `claimedByWorkerId`, `errorMessage`, `requestedByPrincipalId`, `sourceArchiveStudentId`, model output, OCR/RAG, Swarm, tools, direct JS database access, and new writes. |
| SDD 0355 student app ai tutor request progress detail | Student app, AI tutor, personalized question bank, student archive safety, mobile progress UI | Adds `GET /v1/student-app/ai-tutor-requests/{requestId}` as a safe single-request progress detail that consumes READY 0354 list progress evidence; domain scopes the read by Student App principal, `sourceArchiveOwnerType=STUDENT`, own student id, and normalized request id before repository access, then returns the same safe progress card while hiding cross-student requests as not found and blocking `resultRef`, `claimedByWorkerId`, `errorMessage`, `requestedByPrincipalId`, `sourceArchiveStudentId`, internal lineage ids, model output, OCR/RAG, Swarm, direct JS database access, and new writes. |
| SDD 0356 student app ai tutor request progress primary action | Student app, AI tutor, personalized question bank, student archive safety, mobile progress UI | Adds a server-driven `primaryAction` to the safe Student App AI Tutor progress response so mobile clients can render the next safe action without duplicating rules: result-ready points to rendered result archive read, question-bank-ready points to question bank draft content read with normalized draft ref, waiting/review states carry no endpoint or method, and the path still blocks `resultRef`, `claimedByWorkerId`, `errorMessage`, internal lineage ids, model output, OCR/RAG, Swarm, direct JS database access, and new writes. |
| SDD 0357 student app ai tutor request progress target url | Student app, AI tutor, personalized question bank, student archive safety, mobile progress UI | Adds server-generated `primaryAction.targetUrl` to the safe progress response so Student App clients can call the next safe read directly without assembling URLs: result-ready equals the rendered result archive endpoint, question-bank-ready includes URL-encoded `questionBankDraftRef`, waiting/review states expose no URL, and the path still blocks `resultRef`, `claimedByWorkerId`, `errorMessage`, internal lineage ids, model output, OCR/RAG, Swarm, direct JS database access, and new writes. |
| SDD 0358 student app ai tutor request progress refresh policy | Student app, AI tutor, personalized question bank, student archive safety, mobile progress UI, read-path load control | Adds server-owned `refreshPolicy` metadata to the safe progress response so mobile clients stop guessing polling cadence: queued waits 8000ms for worker claim, in-progress waits 5000ms for review/result, result-ready/question-bank-ready stops polling with `ACTION_READY`, teacher-review/failed stops polling with `TEACHER_REVIEW_REQUIRED`, and the path remains read-only while blocking worker ids, internal errors, raw refs, lineage ids, model output, OCR/RAG, Swarm, direct JS database access, and new writes. |
| SDD 0359 student app ai tutor request progress conditional cache | Student app, AI tutor, personalized question bank, student archive safety, mobile progress UI, read-path load control | Adds private conditional-read validators to the safe progress list/detail endpoints: 200 responses return `ETag`, `Cache-Control: private, no-cache`, and `Vary: X-Principal-Context, X-Agent-Api-Key`; matching `If-None-Match` returns `304 Not Modified` with no body. This reduces repeated polling response bytes while preserving student-private scope and blocking shared public caching, worker ids, internal errors, raw refs, lineage ids, model output, OCR/RAG, Swarm, direct JS database access, and new writes; it does not claim to remove the database read or server-side JSON encoding. |
| SDD 0360 student app ai tutor request progress pre-encode validator | Student app, AI tutor, personalized question bank, student archive safety, mobile progress UI, read-path load control | Moves the private conditional-read decision one step earlier for safe progress list/detail responses: the HTTP adapter computes a stable validator from every visible progress card and pageInfo field, and matching `If-None-Match` returns `304 Not Modified` before response mapping and JSON encoding. This reduces repeated polling CPU/allocation while preserving private cache headers and blocking shared public caching, worker ids, internal errors, raw refs, lineage ids, model output, OCR/RAG, Swarm, direct JS database access, and new writes; it does not skip the use-case or database read and does not add Redis, shared cache, writes, model inference, OCR/RAG, or Swarm. |
| SDD 0361 student app ai tutor request progress summary | Student app, AI tutor, personalized question bank, student archive safety, mobile progress UI, read-path ergonomics | Adds a required safe `summary` object to the Student App AI Tutor progress list so the mobile shell can show total, auto-refreshing, action-ready, teacher-review, and failed counts without reimplementing card-state rules client-side. The summary is derived only from already sanitized progress cards after authorization and scoped reads; it keeps `data` and `pageInfo`, bumps the list ETag seed to v2, leaves detail ETag at v1, and still blocks worker ids, internal errors, raw refs, lineage ids, model output, OCR/RAG, Swarm, direct database details, shared cache changes, and write-path changes. |
| SDD 0362 student app ai tutor request progress view filter | Student app, AI tutor, personalized question bank, student archive safety, mobile progress UI, read-path load control | Adds an optional server-owned `progressView` filter to `GET /v1/student-app/ai-tutor-requests` so the Student App can fetch `ALL`, `AUTO_REFRESH`, `ACTION_READY`, `TEACHER_REVIEW_REQUIRED`, or `FAILED` views without duplicating progress-state rules. The domain normalizes the view, rejects ambiguous `status` + non-`ALL` `progressView`, maps views to safe status arrays, pushes the predicate into PostgreSQL, preserves `data`/`pageInfo`/`summary`/private ETag behavior, and still blocks worker ids, internal errors, raw refs, lineage ids, model output, OCR/RAG, Swarm, shared cache changes, database schema changes, and write-path changes. |
| SDD 0363 student app ai tutor request progress summary count | Student app, AI tutor, personalized question bank, student archive safety, mobile progress UI, read-path load control | Adds `GET /v1/student-app/ai-tutor-requests/summary` as a count-only Student App home-badge endpoint. The use case authorizes the same own-student progress scope, then asks PostgreSQL for `status, COUNT(*) GROUP BY status` instead of loading request rows or building progress cards; the response returns only `summary` with private ETag/cache headers and still blocks `data`, `pageInfo`, worker ids, internal errors, raw refs, lineage ids, model output, OCR/RAG, Swarm, shared cache changes, schema changes, and write-path changes. |
| SDD 0364 student app archive items search summary | Student app, teaching materials, student archive, read-path load control | Adds `GET /v1/student-app/archive-items/summary` as a count-only published archive material summary endpoint. It keeps the same own-student publication projection and safe title/tag search scope, then asks PostgreSQL for `material_type, COUNT(*) GROUP BY material_type` instead of loading material rows; the response returns only `summary` with private ETag/cache headers and blocks `data`, `pageInfo`, item IDs, content refs, publication internals, worker state, model output, OCR/RAG, and Swarm. |
| SDD 0365 structure verifier required file registry | Packaging and runtime, strict quality gates | Extracts the large core required-file registry from `tools/verify-structure.mjs` into `tools/verify-structure-required-files.mjs` while keeping a static audit hook for legacy scanners. This is behavior-preserving for structure verification and keeps future SDD/TDD slices below the strict source quality headroom. |
| SDD 0366 student app question-bank drafts summary | Student app, AI tutor, personalized question bank, read-path load control | Adds `GET /v1/student-app/question-bank-drafts/summary` as a count-only Student App home-badge endpoint. The use case authorizes the same own-student draft scope, then asks PostgreSQL for `source_archive_material, COUNT(*) GROUP BY source_archive_material` over succeeded requests with `question_bank_draft_ref IS NOT NULL`; the response returns only `summary` with private ETag/cache headers and blocks `data`, `pageInfo`, request IDs, archive item IDs, `resultRef`, `questionBankDraftRef`, student IDs, worker state, model output, OCR/RAG, and Swarm. |
| SDD 0367 student app summary fast-path flow audit | Student app, mobile home/badge reads, read-path load control, strict quality gates | Extends the cross-module Student App flow audit so `GET /v1/student-app/ai-tutor-requests/summary`, `GET /v1/student-app/archive-items/summary`, and `GET /v1/student-app/question-bank-drafts/summary` are checked as protected fast paths. The gate now requires each summary path to exist, keep `AgentApiKey` + `PrincipalContextHeader`, retain private ETag/304 cache semantics, expose only `summary`, and avoid list fields, row identifiers, student IDs, worker state, refs, model output, OCR/RAG, and Swarm. |
| SDD 0368 student app question-bank draft answer ai feedback read | Student app, AI tutor, personalized question bank, student feedback safety | Adds `GET /v1/student-app/question-bank-draft-answer-submissions/{submissionId}/ai-feedback` as the student-facing safe read for reviewed and archived answer feedback. The use case scopes by Student App own-student principal, verifies the student's submission, reads the latest safe feedback archive snapshot, validates the physical feedback archive row through `ArchiveRepository.GetByID`, and returns only score summary plus safe learner feedback while blocking submitted answers, answer keys, explanations, `contentRef`, `resultRef`, raw model output, prompts, worker state, direct model/OCR/RAG/Swarm access, and database details. |
| SDD 0369 student app question-bank draft answer ai feedback render | Student app, AI tutor, personalized question bank, student feedback safety, mobile render envelope | Adds `GET /v1/student-app/question-bank-draft-answer-submissions/{submissionId}/ai-feedback/rendered` as the safe render envelope for reviewed and archived answer feedback. The renderer reuses the 0368 safe feedback reader, emits deterministic `SAFE_TEXT_BLOCKS`, and blocks submitted answers, answer keys, explanations, `contentRef`, `resultRef`, raw model output, prompts, worker state, rendered HTML/Markdown, direct model/OCR/RAG/Swarm access, and database details. |
| SDD 0370 student app question-bank draft answer ai feedback learning actions | Student app, AI tutor, personalized question bank, student feedback safety, queue admission | Adds `GET /v1/student-app/question-bank-draft-answer-submissions/{submissionId}/ai-feedback/learning-actions` and `QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` source validation so reviewed feedback can safely lead to `POST /v1/student-app/ai-tutor-requests`; queue admission and worker input both rebuild the safe feedback snapshot, render envelope, and action source while blocking raw answers, answer keys, `contentRef`, `resultRef`, model output, prompts, OCR/RAG, Swarm, and database internals. |
| SDD 0371 student app ai tutor question-bank feedback model execution precheck | Student app, AI tutor, personalized question bank, student feedback safety, model admission | Extends the shared AI Tutor model execution precheck runtime so `QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` worker-safe input can record queue-only model admission after 0370; the port receives only source type, render format, safe block count, block digests, input hash, approval, and evidence refs, while feedback text, submission ids, source archive ids, raw answers, answer keys, prompts, model output, OCR/RAG, Swarm, HTTP, and direct DB access remain blocked. |
| SDD 0372 student app ai tutor question-bank feedback controlled answer artifact | Student app, AI tutor, personalized question bank, student feedback safety, controlled answer artifact | Extends the shared controlled answer artifact runtime so `QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` model precheck evidence from 0371 can record a review-only controlled answer artifact; the port receives only safe artifact metadata and evidence refs, while feedback text, feedback submission ids, source archive ids, raw answers, answer keys, prompts, model output, OCR/RAG, Swarm, HTTP, direct DB access, and student-visible publication remain blocked. |
| SDD 0373 student app ai tutor question-bank feedback answer review gate | Student app, AI tutor, personalized question bank, student feedback safety, human review | Extends the shared answer review gate runtime so `QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` controlled answer artifact evidence from 0372 can record a teacher/admin human review gate; the port receives only reviewer metadata, artifact ids, section hash, feedback status, and evidence refs, while guidance text, feedback submission ids, source archive ids, raw answers, answer keys, prompts, model output, OCR/RAG, Swarm, HTTP, direct DB access, tutoring result persistence, and student-visible publication remain blocked. |
| SDD 0374 student app ai tutor question-bank feedback reviewed result persistence bridge | Student app, AI tutor, personalized question bank, student feedback safety, result persistence | Extends the shared reviewed result persistence bridge so `QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` answer review gate evidence from 0373 can record `STUDENT_APP_AI_TUTOR_QUESTION_BANK_FEEDBACK_REVIEWED_RESULT_PERSISTED` through the existing `RecordTutoringAnalysisResult.Execute` use case; output preserves `learningActionSource=QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` and `feedbackStatus=READY_FOR_STUDENT_APP_READ` while blocking guidance text leakage, feedback ids, student-visible publication, raw model output, prompts, answer keys, raw refs, direct DB/HTTP access, OCR/RAG, tools, or Swarm. |
| SDD 0375 student app ai tutor question-bank feedback student visibility review | Student app, AI tutor, personalized question bank, student feedback safety, student visibility | Extends the shared student visibility review runtime so 0374 `QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` reviewed-result persistence evidence can record `STUDENT_APP_AI_TUTOR_QUESTION_BANK_FEEDBACK_STUDENT_VISIBILITY_REVIEW_RECORDED`; output preserves `learningActionSource=QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` and `feedbackStatus=READY_FOR_STUDENT_APP_READ` while approving only a future delivery runtime and blocking guidance text leakage, feedback ids, delivery envelope creation, student-visible publication, raw model output, prompts, answer keys, raw refs, direct DB/HTTP access, OCR/RAG, tools, or Swarm. |
| SDD 0376 student app ai tutor question-bank feedback student delivery envelope | Student app, AI tutor, personalized question bank, student feedback safety, delivery envelope | Extends the shared student delivery envelope runtime so 0375 visibility approval and 0372 safe controlled guidance can record `STUDENT_APP_AI_TUTOR_QUESTION_BANK_FEEDBACK_STUDENT_DELIVERY_ENVELOPE_READY_NOT_PERSISTED`; output preserves `learningActionSource=QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` and `feedbackStatus=READY_FOR_STUDENT_APP_READ` while allowing only a Student App renderable envelope and blocking durable archive persistence, feedback ids, raw refs, raw model output, prompts, answer keys, direct DB/HTTP access, OCR/RAG, tools, or Swarm. |
| SDD 0377 student app ai tutor question-bank feedback student archive persistence command | Student app, AI tutor, personalized question bank, student feedback safety, student archive safety | Adds a question-bank-feedback-sourced student archive persistence command audit that consumes READY 0376 and READY 0372 evidence and records `STUDENT_APP_AI_TUTOR_QUESTION_BANK_FEEDBACK_STUDENT_ARCHIVE_PERSISTENCE_COMMAND_RECORDED_NOT_COMMITTED` through the shared `student_app_ai_tutor_result_student_archive_persistence_command_runtime`; output preserves `learningActionSource=QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` and `feedbackStatus=READY_FOR_STUDENT_APP_READ`, records only an append-only command, and blocks durable storage commit, feedback ids, source archive ids, raw refs, raw model output, prompts, answer keys, direct DB/HTTP access, OCR/RAG, tools, or Swarm. |
| SDD 0378 student app ai tutor question-bank feedback student archive storage commit | Student app, AI tutor, personalized question bank, student feedback safety, student archive safety | Adds a question-bank-feedback-sourced student archive storage commit audit that consumes READY 0377 evidence and commits through the shared `student_app_ai_tutor_result_student_archive_storage_commit_runtime` plus injected `TeachingArchiveCreateItemPort.createArchiveItem`; output preserves `learningActionSource=QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` and `feedbackStatus=READY_FOR_STUDENT_APP_READ`, records `STUDENT_APP_AI_TUTOR_QUESTION_BANK_FEEDBACK_STUDENT_ARCHIVE_STORAGE_COMMITTED`, and blocks physical row verification, feedback ids, source archive ids, raw refs, raw model output, prompts, answer keys, direct DB/HTTP access, OCR/RAG, tools, or Swarm. |
| SDD 0379 student app ai tutor question-bank feedback student archive row verification | Student app, AI tutor, personalized question bank, student feedback safety, student archive safety | Adds a question-bank-feedback-sourced student archive physical row verification audit that consumes READY 0378 evidence and verifies `tarch_student_feedback_001` through the shared `student_app_ai_tutor_result_student_archive_row_verification_runtime` via injected `TeachingArchiveRowReadPort.getArchiveItemById`; output records `STUDENT_APP_AI_TUTOR_QUESTION_BANK_FEEDBACK_STUDENT_ARCHIVE_PHYSICAL_ROW_VERIFIED`, preserves `learningActionSource=QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` and `feedbackStatus=READY_FOR_STUDENT_APP_READ`, requires `ArchiveRepository.GetByID` row-shape evidence, blocks direct DB/HTTP/model/RAG/tool/Swarm access, and leaves safe Student App read/render/learning-actions to the next reviewed slices. |
| SDD 0380 student app ai tutor question-bank feedback student archive read | Student app, AI tutor, personalized question bank, student feedback safety, student archive safety | Adds a question-bank-feedback-sourced Student App safe read audit that consumes READY 0379 evidence and reads `tarch_student_feedback_001` through the shared `student_app_ai_tutor_result_student_archive_read_runtime` via injected `StudentAppAITutorResultArchiveReadPort.readStudentVisibleArchivedResult`; output records `STUDENT_APP_AI_TUTOR_QUESTION_BANK_FEEDBACK_STUDENT_ARCHIVE_READ_VERIFIED`, preserves `learningActionSource=QUESTION_BANK_DRAFT_ANSWER_FEEDBACK` and `feedbackStatus=READY_FOR_STUDENT_APP_READ`, reuses `ReadStudentAppAITutorResultArchive.Execute` and the existing `GET /v1/student-app/archive-items/{archiveItemId}/ai-tutor-result` contract, blocks `contentRef`, feedback ids, raw refs, raw model output, prompts, answer keys, direct DB/HTTP/model/RAG/tool/Swarm access, and leaves safe render to the next reviewed slice. |
